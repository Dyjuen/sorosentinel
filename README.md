# sorosentinel
SoroSentinel is a Rust-based chaos engineering proxy for Soroban DApps. It intercepts traffic to inject real Stellar mainnet failures (RPC drops, sequence collisions, fee drifts) via 12+ pre-built YAML fault profiles. With native GitHub Actions and JUnit reporting, test your DApp's resilience directly inside your CI/CD pipeline.
